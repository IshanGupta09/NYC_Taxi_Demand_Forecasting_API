# ── Raw data (ALWAYS ignored — 1.6 GB, never commit) ──────────────
data/raw/
*.parquet

# ── OPTION A: uncomment these to ignore model + cache ─────────────
# models/
# data/cache/

# ── OPTION B (active): commit model + small cache ─────────────────
# models/xgb_demand_model.pkl  → ~5 MB  — committed ✅
# models/model_meta.json       → <1 KB  — committed ✅
# data/cache/eda_stats.npz     → ~1 KB  — committed ✅
# data/cache/train_features.parquet → ~50 MB — TOO LARGE, ignored
data/cache/train_features.parquet
mlruns/

# ── Python ────────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# ── Testing artifacts ─────────────────────────────────────────────
.coverage
coverage.xml
.pytest_cache/
htmlcov/

# ── IDE ───────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Notebooks checkpoints & generated plots ───────────────────────
.ipynb_checkpoints/
notebooks/*.png

# ── OS ────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Environment ───────────────────────────────────────────────────
.env
.env.local
